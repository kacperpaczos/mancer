# Placeholder: pomocniki do budowania komend (np. fluent API po stronie interface).
# Obecnie komendy buduje się przez ShellRunner.create_command(...) i metody .with_option()/.add_arg() na komendach z infrastructure.
