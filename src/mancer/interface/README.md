# Warstwa Interface (DDD)

Entrypointy użytkownika: CLI, API, pomocniki do budowania komend.

**Stan:** Placeholder. Obecnie głównym punktem wejścia jest `mancer.application.shell_runner.ShellRunner` (użycie programowe). Moduły `cli_interface`, `api_interface`, `command_builder` są zarezerwowane na przyszłą implementację CLI/API i nie są jeszcze zaimplementowane.
